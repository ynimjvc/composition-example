import React from 'react';

export default ({children}) => (
    <div className="metro">
        <div className="pic"/>
        <div className="card-content">
            {children}
        </div>
    </div>
);
